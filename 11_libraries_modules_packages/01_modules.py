# Modules
# Defined module: a self-made module
from utils.math import addition  # Sum is a module that exposes math functions

# ? __pycache__
# This folder cotains compilation files of the module
# this is for improved develepment performance

result = addition(1, 3)
print(result)
