from my_package import messages, utils

# The main purpose of packages is to reuse functionality accross different applications

print(messages.greet("Juan"))
print(messages.bye("Maria"))
print(utils.sum(5, 5))
