# Sets

# ? Unordered collection (not indexes)
# ? Dont allow duplicates
# ? Optimal for math operations
# ? Sets are mutable

# ? Automatically order the values ascending for number sets

my_set = {4, 1, 2, 2, 3, 3, 50, 6, 5, 8, 7, 7}

print(my_set)
