numbers = [1, 2, 3, 4, 5]

# * Return first index of value.
index_of_five = numbers.index(5)

print(index_of_five)

# * Can also pass an start and end position to perform the search
# index_of_five = numbers.index(5, 0, 4)

# print(index_of_five)

# IN to know if an element in IN the list
print(10 in numbers)

# How many times the element appear is in the list
element = 2
print(numbers.count(element))
